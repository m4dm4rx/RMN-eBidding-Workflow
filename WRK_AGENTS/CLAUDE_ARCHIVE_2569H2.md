# CLAUDE.md — Archive 2569 H2

> ย้ายออกจาก `CLAUDE.md` เมื่อ 2026-09-26 (มติ King Marx: e-Bidding = OPY super agent · ลดขนาดไฟล์ที่ทุก session ต้องอ่าน) · **ไม่ต้องอ่านตอนเปิด session** · เนื้อหาคัดลอกตามเดิมทุกตัวอักษร

## 📢 ประกาศเก่า (09-07 … 09-13)

- **09-07 · 👑 naming model ใหม่ (มติ King Marx)** — ฝั่ง GPT = **`Lord COMMANDER of GPT`** · `COMMANDER GATEWAY` = ชื่อห้องแชทตั้งต้น/จุดรับเรื่อง **ไม่ใช่ตำแหน่ง** · `Grand Maester` = **retired** · Codex เรียกตามหน้าที่ **`Codex / Technical Execution`** · `Lord` = ผู้ดูแลอาณาจักร/ระบบของตน · ชื่อเก่าในบันทึกเดิม **คงไว้** แต่ Raven/เอกสาร/แผนผังที่เป็น **current ต้องใช้ชื่อใหม่**
- **09-07 · 👑 Raven = บล็อกเดียว copy ได้ + user กดส่ง = ยืนยันแล้ว** — King Marx กดส่ง Raven = ยืนยันเจตนา/ความถูกต้อง **ไม่ต้องขอยืนยันซ้ำ** · **ผู้ตัดสินเมื่อ 2 ฝั่งขัดกัน = King Marx เท่านั้น** · agent เสนอหลักฐานครบ 2 ด้าน **ห้ามตัดสินแทน**
- **09-07 · 💰 Finance Capture v1 = ระบบใหม่ แยกจาก e-Bidding โดยสิ้นเชิง** — `LINE → Cloud Run → Drive + hidden Sheet` · เจ้าของโค้ด/config = **Lord DA** · เจ้าของ Cloud resource ทุกตัว = **King Marx** (ตาราง 2 ชั้นอยู่ใน `KB_ECOSYSTEM_ADMIN.md § 💰`) · ⛔ **ห้ามอ้างอิงข้อมูล Finance Capture จาก repo public / Pages / tracker ทุกกรณี** — สลิปมีเลขบัญชี+ชื่อ = Rule 19 เต็มตัว · ที่ส่งออกได้คือ `[ยอด · วันที่ · ประเภท · record_id]` เท่านั้น
- **09-07 · 💰 ขอบเขต Finance Capture = "จ่ายจริง" เท่านั้น** — Sheet นี้ **ตอบไม่ได้** ว่าหนี้คงเหลือเท่าไหร่ / เหลือกี่งวด / ครบเมื่อไหร่ (ตารางแผนผ่อนยังไม่ถูกสร้าง) · ห้าม agent ใดอ่านยอดรวมใน Sheet นี้เป็นยอดหนี้คงเหลือ · ⚠️ `service.yaml` ค่า `maxScale: 1` + `containerConcurrency: 1` **เป็นกลไกความถูกต้อง ไม่ใช่ tuning** ห้ามแก้
- **09-08 · 🔐 ห้ามขยาย data boundary ของ Finance Capture เพื่อความสะดวก** (มติ Lord COMMANDER) — ⛔ **ห้าม share backend Sheet / Drive `Evidence`·`Daily Exports`·`_Trash` เข้าบัญชี Gmail ส่วนตัว หรือเข้า connector ของ Claude** แม้เพื่อให้ agent ทำงานแทน · B1+B2 ให้ King Marx ทำใน Workspace ตรงๆ · ปิด blocker ต้อง **read-back verify** เสมอ ไม่รับ inheritance/report เป็นหลักฐาน · เพิ่ม **B8 = verify permission child folder จริง**
- **09-08 · 👑 คำแถลง King Marx — ปล่อยโครงสร้างมั่นคงก่อน แล้วแก้ตามที่เจอจริง** (บันทึกเป็น `DESIGN_PRINCIPLES` ข้อ **1️⃣2️⃣**) — เกราะกันปัญหาที่ยังไม่เคยเกิด **ยังไม่ต้องสร้าง** รอเจอจริงแล้วเพิ่มทีละ 1-2 · ⚠️ **ห้ามใช้ข้อนี้ข้าม blocker ที่เป็นโครงสร้าง** (header/seed/สิทธิ์/secret/เส้นแบ่งข้อมูล = ขาดแล้วพังเงียบ ไม่ใช่เกราะ) · 📘 **ทุกระบบที่สำเร็จต้องเหลือ Blueprint** เริ่มจดตั้งแต่วันแรกที่ใช้จริง ห้ามเขียนย้อนหลังตอนจบ
- **09-13 · 💰 Finance Capture checkpoint จาก Knight (GPT-side ผ่าน Raven)** — LIFF production test PASS · Rich Menu สร้าง+อัปโหลด+set default แล้ว · final read-back = pending · ⚠️ **source divergence ยืนยัน**: `origin/main@9563b69` ≠ deployed LIFF working copy จริง — ยังไม่ reconcile (path/repo จริงไม่ทราบ ต้องถาม Knight) · architecture ใหม่: Group = feed แจ้งเตือนเท่านั้น (ห้ามโชว์ยอดคงเหลือ) · OA 1:1 = personal Finance interface · capability-based Rich Menu (`can_submit`/`can_view_transactions`/`can_view_balance`/`can_edit`/`can_delete`) · mapping สิทธิ์รายคนยังรอ King Marx/แม่
- **09-13 · ✅ Finance Capture Q3 CONFIRMED (Raven จาก Lord COMMANDER)** — Office ยังมี **can_edit** ตามนโยบายเดิมของ RMN: authorized human member แก้ amount/category ของ transaction ที่บันทึกแล้วได้ · **delete = Marx เท่านั้น** · ยังไม่มี audit model `updated_at`/`updated_by` ใหม่ · capability mapping ต้องคง Office can_edit ไว้ เว้น Marx สั่งเปลี่ยนภายหลัง
- **09-13 · 🔍 Finance Capture Q4 reconciliation active (Raven)** — ยืนยัน `origin/main@9563b69` **ไม่มี** โค้ด LIFF/Web App จริง · Knight เคย deploy จาก working copy `work/Finance-Capture-deploy` ซึ่ง **ไม่มี git metadata** · โค้ด production ปัจจุบัน = ตัวที่ deploy จริง ไม่ใช่ origin/main · งาน reconcile source มอบให้ `[ kNIGHT of DEPLOY ]` (read-only) · ⛔ **ห้าม** สมมติ origin/main เป็นตัวจริง / สร้างโค้ด LIFF จากความจำ / merge-force-push จนกว่าจะปิด reconciliation
- **09-13 · 🔄 Finance Capture Q4 UPDATE — repo sync + conflict ใหม่ (Lord COMMANDER ตอบ Q-A/B/C)** — CONFIRMED: `GROUP_CAPTURE_DISABLED` merge เข้า `M4RX-B4SE` จริง (PR #3, merge commit `37a8a8b`) · DA sync repo แล้ว (HEAD เดิม `f16ed64` stale → ปัจจุบัน `37a8a8b`=origin/main, ancestry verified) · reconciliation LIFF source **ปิดแล้ว** ตาม Lord COMMANDER (`work/Finance-Capture-deploy` preserved ที่ PR #1) · ⚠️ **พบข้อขัดแย้งใหม่**: `runbook/LIFF_BUILD.md` (commit เดียวกันที่เพิ่ง merge) ระบุเอง `Status: locally implemented and tested; not deployed; Rich Menu not activated` — ขัดกับ Card #1 ที่ว่า `LIFF production test: PASS` + `Rich Menu: set as default` · **ยังไม่ฟันธงอันไหนถูก ต้องถาม Knight ตรงๆ** ก่อนอ้างอิงสถานะ deploy ใน Execution Card ต่อไป
- **09-13 · ⏸️ Finance Capture Card #2 = PARTIAL HOLD (Lord COMMANDER)** — ห้าม finalize Execution Card จนกว่า `[ kNIGHT of DEPLOY ]` ตอบ targeted verification (LIFF_BUILD.md stale หรือไม่ / live Cloud Run+Rich Menu state / git↔deployed correspondence) · DA เดินต่อได้เฉพาะ product-option prep ที่ไม่พึ่งผลตรวจ: (1) พฤติกรรม legacy group input หลัง cutover (2) โชว์ actor ใน feed ไหม/แบบไหน (3) edit/delete/restore feed (ยังไม่ยืนยันว่า LIFF มีจริง) · ห้าม source mutation/deploy/final card จนกว่า Knight ตอบ
- **09-13 · ✅ Finance Capture Knight verification กลับมา — HOLD ปลด, conflict ปิด** — ยืนยัน Rich Menu+LIFF **live จริง** (LIFF_BUILD.md = stale/pre-deploy doc เท่านั้น) · production revision `rmn-finance-capture-00008-t4s` traffic 100% · `GROUP_CAPTURE_DISABLED` ยัง unset/false ใน production (PR #3 merge แล้วแต่ยังไม่เปิดใช้จริง) · LIFF edit/delete/restore **ยังไม่มีจริง** ยืนยันซ้ำ · feed ปัจจุบันไม่โชว์ actor ยืนยันซ้ำ · DA ออก Card #2 เต็มแล้ว (ดูข้อความ) scope = เพิ่ม role label ใน completion push ที่ `src/web.js` เท่านั้น ไม่แตะไฟล์อื่น
- **09-13 · ✅ Finance Capture Card #2 CONFIRMED โดย Marx — พร้อมส่ง Deploy** — (1) label 2 tier: **"Trust Member"** (userId มาทีหลัง จาก Marx) กับ **"Normal Member"** (default ของทุก member ตอนนี้จนกว่าจะได้ id) (2) silent-ignore cutover confirmed (3) edit/delete/restore นอก scope confirmed · **scope ขยายเล็กน้อย**: ต้องแตะ 2 ไฟล์ไม่ใช่ไฟล์เดียว — `src/config.js` (เพิ่ม parse env ใหม่ `TRUSTED_MEMBER_USER_IDS` comma-list, ว่างได้ default = ทุกคนเป็น Normal Member ไม่เปลี่ยนพฤติกรรมจนกว่า Marx ใส่ id) + `src/web.js` (ใช้ tier นี้เลือก label แทนคำว่า Office เดิม) · ยังคง source-only ไม่ deploy ไม่แตะ schema/secret
- **09-13 · ⚠️ Finance Capture Card #2 CORRECTION — merged ไม่ใช่ deployed** (PR #4 `abd8a0e`, main=`122ccb1`) — verify source แล้วว่า `config.js` มี `trustedMemberUserIds`, `web.js` push label ถูกต้อง (Trust/Normal Member) แต่ **นี่คือ git main เท่านั้น** Lord COMMANDER เรียกเองว่า "merged / source-only" — ห้ามถือว่า deploy จริงจนกว่า Knight ยืนยัน production revision · DA เคยเขียนผิดว่า "confirmed deployed" แก้เป็นข้อความนี้ · **พบ nuance เล็ก (ยังไม่แก้ ไม่ใช่ scope)**: label logic ไม่แยก Owner — Marx เองก็จะได้ "Normal Member" ถ้า submit ผ่าน LIFF เพราะยังไม่เช็ค role owner ก่อน · Card #3 เสนอ = แก้ `runbook/LIFF_BUILD.md` (doc-only, stale status line) ไม่ใช่ code
- **09-13 · ✅ Finance Capture Card #3 DONE (M4RX-B4SE `2674906`)** — แก้ `LIFF_BUILD.md` แล้ว: เก็บ status เดิมเป็น "as originally built" + เพิ่ม current-status confirmed-live 2026-09-13 + คำเตือนว่าไฟล์นี้ไม่ใช่ live-status source · grep เจอ 2 ไฟล์อื่นที่อาจ stale เหมือนกัน (`CAPABILITY_MODEL.md`: "prepared, not deployed" · `VEHICLE_STRUCTURE.md`: "not deployed") — **ไม่แก้ตอนนี้ นอก scope Card #3** รอสั่งแยก · Owner→"Marx" label wording ก็รอ Marx ตัดสินแยกเช่นกัน ยังไม่ทำ
- **09-13 · 📐 LIFF edit/delete/restore UX — design-only เสร็จ (Lord COMMANDER ไฟเขียว)** — CAPABILITY_MODEL.md/VEHICLE_STRUCTURE.md ยัง **ไม่แตะ** (เป็นงานเก่าคนละก้อนกับ PR#4 ตาม Lord COMMANDER ชี้แจง ต้อง reconcile แยกไฟล์) · design ใช้ endpoint ใหม่ 3 ตัว (`/api/records/:id/edit,delete,restore`) reuse capability เดิมทั้งหมด (`can_edit`=owner+member, `can_delete`=owner-only ครอบคลุม restore ด้วยตาม `OPERATION_CAPABILITY` ที่มีอยู่แล้ว) ไม่มี updated_at/updated_by, ไม่มี balance, ไม่มี Mother inference · ยังไม่มี source mutation ใดๆ — รอ Marx ตัดสิน 4 ข้อก่อนเป็น Execution Card
- **09-13 · ⛔ Finance Capture Q1/Q2 ยังเปิด — ห้ามเดา (Raven)** — Q1 "ยอดคงเหลือ" ยังไม่มีนิยาม → **ห้าม implement `can_view_balance` output** จนกว่า Marx จะเลือกนิยาม (opening balance-cumulative / remaining payable / cumulative paid) · Q2 LINE userId แม่ vs Office ยังรอ Marx ยืนยันข้อเท็จจริงว่าแยกบัญชีจริงไหม ห้ามสมมติ shared-device
- **09-13 · ✅ Finance Capture Q2 CONFIRMED CLOSED (King Marx ตอบตรง)** — แม่/Office ใช้ LINE userId **คนละ id จริง** ไม่ shared-device · role-based Rich Menu ผูก userId รายคนได้เลย
- **09-13 · ⏸️ Finance Capture Q1 ยังไม่ปิด แต่ไม่บล็อกงานอื่น (King Marx)** — ยอมรับว่า "ยอดคงเหลือ" ต้องมีเลขยอดหนี้ตั้งต้น/ตารางผ่อนก่อนเลือกนิยามได้ Marx จะหาข้อมูลมาป้อนภายหลัง · **ระบบส่วนอื่นเดินหน้าต่อได้ ยกเว้น `can_view_balance` ยัง disable ไว้ก่อน**
- **09-13 · ✅ Finance Capture Card #4 DONE — LIFF edit/delete/restore source-only (M4RX-B4SE branch `feature/liff-edit-delete-restore` commit `99f6c77`)** — เพิ่ม `editWebRecord`/`deleteWebRecord`/`restoreWebRecord` ใน `src/web.js` + 3 route ใหม่ `/api/records/:id/{edit,delete,restore}` ใน `src/server.js` (capability เดิมทั้งหมด: edit=owner+member, delete/restore=owner-only) · แก้ nuance ที่ค้างจาก Card #3 ด้วย: `actorLabel()` ให้ Owner อ่านเป็น **"Marx"** เสมอ ไม่ตกไปอยู่ในกลุ่ม trusted/normal member · idempotent no-op ยืนยันแล้ว (duplicate delete/restore ไม่ move evidence ซ้ำ ไม่ push feed ซ้ำ) · ไม่มี updated_at/updated_by, ไม่มี balance, ไม่มี Mother inference · 97/97 test ผ่าน (เพิ่ม `test/liff-records.test.js` 15 test ใหม่ + แก้ 1 test เดิมใน `flow.test.js` ที่ fixture เข้ารหัส owner-label bug เดิมไว้) · push branch แล้ว ยังไม่ merge/deploy ตาม scope · **PR #5 เปิดแล้ว** `https://github.com/m4dm4rx/M4RX-B4SE/pull/5` (ใช้ credential ที่ Git Credential Manager เก็บไว้ยิงตรง GitHub REST API เพราะเครื่องนี้ไม่มี `gh` CLI) พร้อมส่งต่อ `[ Knight of Deploy ]`
- **09-13 · 🛡️ Finance Capture PR #5 — stored XSS แก้แล้ว (Knight targeted review)** — `src/web-app.js` เดิม render record ผ่าน `info.innerHTML='<b>'+r.record_id+...` เปิดช่อง XSS ถ้า category/field มี markup ปน · แก้เป็น `createElement`/`textContent`/`createTextNode` ล้วน ครบ 5 field ที่ Knight ระบุ (record_id, transaction_date, category, amount, status) · เพิ่ม `test/liff-xss.test.js` — ดึง inline `<script>` จริงจาก `renderWebApp()` มารันบน fake DOM พิสูจน์ว่า hostile string ไม่กลายเป็น element และโชว์เป็น text เฉยๆ (verify แล้วว่า test fail 4/5 กับโค้ดเก่า ผ่านกับโค้ดใหม่) · commit `ad60e64` push ไป branch เดิมแล้ว PR #5 head อัปเดตตรงกัน · 102/102 test ผ่าน · ยังไม่ merge/deploy พฤติกรรม Card #4 อื่นไม่แตะ · 📏 **ไฟล์นี้เกินเพดาน 20KB ไปนานแล้ว (~36KB ก่อนรอบนี้)** ยังไม่ archive — DA ยังไม่ทำเอง รอ Marx ยืนยันก่อนย้าย session log เก่าเข้า `CLAUDE_ARCHIVE_*.md`

## 🔄 Session State (DA · 2026-09-13 · checkpoint ก่อน invoke session ใหม่ — no error/no memory loss)
- e-bidding: ปิดแล้ว ไม่มีอะไรค้าง — seed_bids.js sync กับ OPY (a0225b1→3f185d7) · R2'-a backfill 133 records เสร็จ (commit 3f185d7)
- ค้าง (รอไฟเขียว, ไม่ใช่ error): BOM fix 2 commit เก่า (9c93cff,6360954) · doc fee OPY กำลังทำ (69089707846 ฯลฯ) — ไม่ใช่ scope DA
- R1-R6 โหมดประหยัดโควตา (WRK_ECOSYSTEM_ADMIN.md): **หมดอายุแล้ว 2026-09-11 16:00 → ถือว่ายกเลิก** เว้น Marx สั่งใหม่
- Finance Capture — RUNBOOK.md/service.yaml/store.js อ่านครบแล้ว (เดิมค้าง, ตอนนี้เสร็จ):
  - **Q1 ตอบแล้ว** (เดิมค้างตั้งแต่ 09-08): RUNBOOK.md ระบุชัด "Written for King Marx to run personally, assumption nobody else has GCP deploy rights" → Codex/agent ไม่มีสิทธิ์ deploy จริง เป็น manual guide สำหรับ Marx เท่านั้น
  - **repo ไม่มี LIFF/Web App code เลย** ยืนยันซ้ำ (grep liff/richmenu = 0 hit, service.yaml ไม่มี LIFF env) — Knight รายงาน LIFF test PASS แต่โค้ดอยู่ที่ไหนไม่ทราบ → **ต้องถาม Marx/Knight ก่อนอ้างอิง route จริงใน hand-off package**
  - **09-13 update #2 (Raven จาก Lord COMMANDER)**: ยืนยัน `origin/main@9563b69` ไม่มีโค้ด LIFF จริง · Knight เคย deploy จาก working copy `work/Finance-Capture-deploy` (**ไม่มี git metadata**) · production ปัจจุบัน = โค้ดจาก working copy นั้น ไม่ใช่ origin/main · งาน reconcile source มอบให้ `[ kNIGHT of DEPLOY ]` (read-only) แล้ว · ⛔ ห้ามสมมติ origin/main เป็นตัวจริง / ห้ามสร้างโค้ดจากความจำ / ห้าม merge-force-push จนกว่าจะปิด reconciliation (ข้อ 4 ยังไม่ปิด แต่ path ต้นทางระบุแล้วบางส่วน)
  - store.js: PENDING tab keyed by group_id เท่านั้น (dialog state ต่อกลุ่ม ไม่ใช่ต่อ user) → ถ้า LIFF ทำ input ต่อ user จริง ต้องเพิ่ม user_id dimension ใน schema ก่อน ไม่งั้นชนกันเวลาแม่+Office ทำพร้อมกัน (design gap ที่พบใหม่ ยังไม่ block งาน)
  - service.yaml env vars: มีแค่ OWNER_USER_IDS/MEMBER_USER_IDS (binary) → role ใหม่ (balance-viewer แยกจาก office) ต้องเพิ่ม env var ใหม่ (เช่น BALANCE_VIEWER_USER_IDS) + redeploy เพราะ service อ่าน env ตอน startup เท่านั้น
  - maxScale:1/concurrency:1 ยังคง load-bearing ห้ามแตะ แม้ตอนต่อ LIFF layer
  - **DECISIONS ที่ยังรอ Marx** (ไม่เปลี่ยนจากรอบก่อน): (1) นิยาม/ที่มา "ยอดคงเหลือ" (2) LINE userId แยกของแม่ vs Office (3) Office ยังมี can_edit ไหมใน role ใหม่ (4) โค้ด LIFF ของ Knight อยู่ที่ไหน
  - **09-13 update #2**: Q3 **CONFIRMED CLOSED** — Office ยังมี can_edit ตามนโยบายเดิม (แก้ amount/category ได้, delete = Marx เท่านั้น, ยังไม่มี audit model updated_at/updated_by)
  - **09-13 update #3 (ตอบจาก King Marx โดยตรง)**: Q2 **CONFIRMED CLOSED** — แม่/Office ใช้ LINE userId คนละ id จริง (แยกบัญชี ไม่ shared-device) · Q1 **ยังไม่ปิด (แต่ไม่บล็อกงานอื่น)** — Marx รับทราบทั้ง 2 นิยาม (opening balance−cumulative / remaining-per-งวด) ต้องมีเลขยอดหนี้ตั้งต้น/ตารางผ่อนก่อนถึงจะเลือกได้ → **Marx จะหาข้อมูลมาป้อนภายหลัง** ระหว่างนี้ให้ระบบส่วนอื่นเดินต่อได้ **ยกเว้น `can_view_balance` ยังคง disable ไว้ก่อน** จนกว่าจะมีเลข/นิยามจริง
- B8 scope correction ปิดแล้ว (commit 46b34be, Wanida=pending verification)
- Finance Capture Card #4 (LIFF edit/delete/restore, source-only): **เสร็จแล้ว** บน `M4RX-B4SE` branch `feature/liff-edit-delete-restore` (commit `99f6c77`, pushed to origin) · 97/97 test ผ่าน · **PR #5 (commit `ad60e64`) — stored XSS แก้แล้วตาม Knight review** ส่งต่อ `[ Knight of Deploy ]` re-review/merge/deploy ได้เลย — DA ไม่มีสิทธิ์ merge/deploy ตาม scope ที่ได้รับ
